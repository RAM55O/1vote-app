version: "3.8"

services:
  vote-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: 1vote-app-service
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
      - SERVER_PORT=8080
    restart: unless-stopped
